import 'package:flutter/material.dart';

class TransactionHoistory extends StatelessWidget {
  const TransactionHoistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('This is a Treansaction History Page'),
    );
  }
}
