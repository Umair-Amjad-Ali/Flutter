import 'package:flutter/material.dart';

class MissionReward extends StatelessWidget {
  const MissionReward({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Mission and Reward"),
    );
  }
}
