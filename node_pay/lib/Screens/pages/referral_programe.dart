import 'package:flutter/material.dart';

class ReferralPrograme extends StatelessWidget {
  const ReferralPrograme({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('This is a Referral Programme Page'),
    );
  }
}
